# RADIUS clients — add your NAS devices here.
# Each client needs a unique name, IP/subnet, and shared secret.

client dockernet {
	ipaddr = 172.16.0.0/12
	secret = ${RADIUS_SECRET:-testing123}
}

client localhost {
	ipaddr = 127.0.0.1/32
	secret = testing123
}
